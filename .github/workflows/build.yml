name: build

on: [push]

jobs:
  build:

    runs-on: macos-11

    steps:
      - uses: actions/checkout@v2
      - name: Choose version
        run: swift -version
      - name: Build
        run: swift build
      - name: Run tests
        run: swift test --filter AmadeusTests.AmadeusTests